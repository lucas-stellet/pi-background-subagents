{
  "name": "pi-background-subagents",
  "version": "0.1.0",
  "description": "A Pi extension that runs subagents in the background and notifies the parent agent when results are ready.",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "check": "tsc --noEmit --allowImportingTsExtensions --skipLibCheck --module nodenext --moduleResolution nodenext --target es2022 --types node index.ts agents.ts job-storage.ts chain-lock.ts chain-resume.ts chains.ts intercom.ts tests/*.test.ts"
  },
  "files": [
    "index.ts",
    "agents.ts",
    "job-storage.ts",
    "chain-lock.ts",
    "chain-resume.ts",
    "chains.ts",
    "intercom.ts",
    "examples",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "pi-package",
    "pi-extension",
    "subagents",
    "background-agents"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
